{{> header}}
{{> navbar}}
    <div class="container">
    <form action="/edit-data/{{heroes.id}}" method="POST">
      <div class="row mt-5 mb-5">
        <div class="col-6">
          <img src="{{heroes.photo}}" class="img-fluid" alt="imgnotfound" />
        </div>
        <div class="col-6 infocars">
          <div class="text-details-content mb-3">
              <div class="text-title-content mb-2">
                <input name="name" type="text" value="{{heroes.name}}">
             </div>
               <input name="type_id" type="text" value="{{heroes.type_id}}">
          </div>
          <div class="text-end">
            <button class="btn-submit btn btn-succes">Done Edit</button>
          </div>
        </div>
      </div>
      </form>>
</div>
{{> footer}}